namespace Reveal3d.Util;

public static class Constants {
    public static class Paths {
        public static readonly string AppData = $@"{Environment.GetFolderPath(Environment.SpecialFolder.ApplicationData)}\RevealEngine";
        public static readonly string DefaultSavesFolder = $@"{Environment.GetFolderPath(Environment.SpecialFolder.MyDocuments)}\RevealEngine";
        //Installation path? 
    }
}